language: python
python:
  - "3.6"
  - "3.7"
  - "3.8"
  - "3.9"
before_install:
  - pip install poetry
# command to install dependencies
install:
  - poetry install
# command to run tests
script: "poetry run python -m pytest"
